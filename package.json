{
  "name": "automation-task-symbhiring",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test-ui": "npx cypress run --spec cypress/integration/ui/*.js --browser chrome --headless",
    "test-api": "npx cypress run --spec cypress/e2e/api/**/*.cy.js --browser chrome --headless"
  },
  "author": "pratapsofficial@gmail.com",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "cypress": "^14.0.0",
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-xpath": "^2.0.1"
  }
}
